CONN EMP_DEPT_MANAGEMENT/123@//LOCALHOST:1523/ORCLDOCKER ;

SELECT COLUMN_NAME,DATA_TYPE,DATA_LENGTH 
FROM USER_TAB_COLUMNS
WHERE TABLE_NAME = 'DEPT' OR TABLE_NAME = 'EMPL';

SELECT DEPT.DEPTNO,COUNT(*) AS SOLUONG
FROM DEPT , EMPL
WHERE DEPT.DEPTNO = EMPL.DEPTNO 
GROUP BY DEPT.DEPTNO
ORDER BY SOLUONG DESC;

CONN SYS/123@//LOCALHOST:1523/ORCLDOCKER AS SYSDBA; 
ALTER SESSION SET "_ORACLE_SCRIPT" = TRUE;
ALTER SESSION SET CURRENT_SCHEMA = EMP_DEPT_MANAGEMENT;
SET AUTOTRACE ON EXPLAIN;
SELECT * FROM DEPT WHERE DEPTNO = 20;

DISCONNECT;