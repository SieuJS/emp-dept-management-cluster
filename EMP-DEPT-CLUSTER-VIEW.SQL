CONN EMP_DEPT_MANAGEMENT/123@//LOCALHOST:1523/ORCLDOCKER ;

ALTER SESSION SET CURRENT_SCHEMA = EMP_DEPT_MANAGEMENT;


SELECT ROWID FROM EMPL WHERE DEPTNO = 10;
SELECT ROWID FROM DEPT WHERE DEPTNO = 10;
SELECT ROWID FROM EMPL_INDEX_CLUS WHERE DEPTNO = 10;
SELECT ROWID FROM DEPT_INDEX_CLUS WHERE DEPTNO = 10;
SELECT ROWID FROM EMPL_HASH_CLUS WHERE DEPTNO = 10;
SELECT ROWID FROM DEPT_HASH_CLUS WHERE DEPTNO = 10;



SELECT CLUSTER_NAME, TABLESPACE_NAME, CLUSTER_TYPE, PCT_INCREASE, MIN_EXTENTS, MAX_EXTENTS FROM USER_CLUSTERS;


SELECT INDEX_NAME, INDEX_TYPE, PCT_INCREASE, MIN_EXTENTS, MAX_EXTENTS FROM USER_INDEXES ;


DISCONNECT;