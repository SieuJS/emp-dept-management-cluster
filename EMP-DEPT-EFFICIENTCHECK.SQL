CONN SYS/123@//LOCALHOST:1523/ORCLDOCKER AS SYSDBA; 

ALTER SESSION SET CURRENT_SCHEMA = EMP_DEPT_MANAGEMENT;

EXPLAIN PLAN FOR SELECT *  FROM EMPL E, DEPT D WHERE E.DEPTNO = D.DEPTNO AND E.DEPTNO = 10 ;

SELECT * FROM TABLE (DBMS_XPLAN.DISPLAY);


EXPLAIN PLAN FOR SELECT * FROM EMPL_INDEX_CLUS E , DEPT_INDEX_CLUS D WHERE D.DEPTNO = E.DEPTNO AND E.DEPTNO = 10 ;

SELECT * FROM TABLE (DBMS_XPLAN.DISPLAY);

EXPLAIN PLAN FOR SELECT * FROM EMPL_HASH_CLUS E, DEPT_HASH_CLUS  D WHERE D.DEPTNO = E.DEPTNO AND E.DEPTNO = 10 ;

SELECT * FROM TABLE (DBMS_XPLAN.DISPLAY);
